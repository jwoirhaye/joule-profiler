# joule-profiler
joule-profiler is a Rust tool that measures a program’s energy consumption using Intel RAPL. It reports CPU, DRAM, Uncore, Package or any other RAPL domains available on the machine, and can export results in terminal, CSV or JSON formats.
